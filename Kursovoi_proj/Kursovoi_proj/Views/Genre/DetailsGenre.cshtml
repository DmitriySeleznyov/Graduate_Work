
@{
    ViewBag.Title = "DetailsGenre";
}

<h2>Информация о Жанрах</h2>

<table class="table">
    <tr>
        <th>Код жанра</th>
        <th>Название жанра</th>
        <th>Действия</th>
    </tr>
    @if (Model != null)
    {
        <tr>
            <td>@Model.Genre_Id</td>
            <td>@Model.Name_Genre</td>
            <td>
                @Html.ActionLink("Delete", "Delete", "Genre", new { id = @Model.Genre_Id }, new { @class = "btn btn-default" })
                @Html.ActionLink("Edit", "Edit", "Genre", new { id = @Model.Genre_Id }, new { @class = "btn btn-default" })
            </td>
        </tr>
    }
</table>
<br>

@Html.ActionLink("Назад", "Index", "Genre", new { @class = "btn btn-default" })

